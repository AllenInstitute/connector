# Connector — core runtime dependencies
#
# PyTorch note: this pulls the default (often CPU-only) wheel. GPU users
# should FIRST install the CUDA build that matches their system by
# following https://pytorch.org/get-started/locally/, then run:
#   pip install -r requirements.txt
# torch>=2.6 is required because the code uses torch.accelerator.

torch>=2.6
numpy>=1.24
scipy>=1.10
scikit-learn>=1.3
matplotlib>=3.7
seaborn>=0.12
tqdm>=4.65

# Project-specific (PyPI only — not available on conda channels):
flow_matching>=1.0
geomloss>=0.2.6

# Jupyter kernel — lets you run the notebooks from VS Code or a separate
# JupyterLab (use `jupyterlab` instead for a self-contained env). After
# installing, register the kernel once:
#   python -m ipykernel install --user --name connector --display-name "Python (connector)"
ipykernel>=6.0
